spring.application.name=inventory-system
server.port=${PORT:8080}
server.address=0.0.0.0

# ================= DATABASE (RAILWAY FIX) =================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:sCCRTyonmHmIbTJduCwlCoTSOJJtALBe}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ================= JWT =================
app.jwt.secret=${JWT_SECRET:9a4f2c8d3e1f0a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b}
app.jwt.expiration-ms=604800000

# ================= FILE UPLOAD =================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/

# ================= MAIL (SMTP - fallback) =================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:vipinchaurasiya1052@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wmyephzfugycenrg}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false

spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.debug=true

spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.writetimeout=30000

# ================= JSON =================
spring.mvc.contentnegotiation.favor-path-extension=false
spring.mvc.converters.preferred-json-mapper=jackson

# ================= MAIL PROVIDERS =================
mail.provider=${MAIL_PROVIDER:SENDGRID}

# SendGrid
sendgrid.api.key=${SENDGRID_API_KEY:}
sendgrid.from.email=${SENDGRID_FROM_EMAIL:parts.mitra05@gmail.com}

# Mailgun
mailgun.api.key=${MAILGUN_API_KEY:}
mailgun.domain=${MAILGUN_DOMAIN:}
mailgun.from.email=${MAILGUN_FROM_EMAIL:}

# Brevo
brevo.api.key=${BREVO_API_KEY:}
brevo.from.email=${BREVO_FROM_EMAIL:}

# ================= OTP =================
app.otp.demo-mode=${DEMO_MODE:false}
otp.mode=${OTP_MODE:EMAIL}

# ================= AI =================
app.gemini.api.key=${GEMINI_API_KEY:}
app.openai.api.key=${OPENAI_API_KEY:}
app.openai.model=${OPENAI_MODEL:gpt-4o-mini}

# ================= FIREBASE =================
firebase.service-account.path=${FIREBASE_SERVICE_ACCOUNT_PATH:}
firebase.service-account.json=${FIREBASE_SERVICE_ACCOUNT_JSON:}

# ================= CORS =================
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://sparehub-0t47.onrender.com,https://spareshub.app,https://sparehub-chi.vercel.app,https://*.vercel.app,http://localhost:5173}